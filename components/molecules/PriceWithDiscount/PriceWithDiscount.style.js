import React from "react";
import styled from "styled-components/native";
import { colors, typography } from "../../../utils";
import { scale } from "react-native-size-matters";

export const PriceWithDiscountWrapper = styled.View`
  flex-direction: row;
  align-items: center;
`;
